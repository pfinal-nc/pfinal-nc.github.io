# 2026-04-17 工作日志

## 接入广告联盟 tag

将 `al5sm.com` 广告 tag（zone=9182859）集成到 `docs/.vitepress/theme/client.js` 的 `loadDeferredAnalyticsAndAds` 函数中，与 GA4/AdSense 一同延迟加载（requestIdleCallback），不阻塞首屏。

commit `da642fbc`，已推送至 `origin/master`。

## Kubernetes 入门到实战系列（4篇）

在 `docs/courses/devops-practice/` 下新增 4 篇 K8s 文章：

1. `k8s-architecture.md` — K8s 架构与核心概念（控制平面、Pod、Node、Namespace、Label、kubectl）
2. `k8s-workloads.md` — 工作负载（Deployment 滚动更新/金丝雀、StatefulSet、DaemonSet、Job/CronJob、探针、优雅停机）
3. `k8s-networking.md` — 服务发现与网络（ClusterIP/NodePort/LoadBalancer/ExternalName、Ingress、CoreDNS、NetworkPolicy）
4. `k8s-config.md` — 配置管理（ConfigMap、Secret、Vault、External Secrets、Sealed Secrets、RBAC）

更新课程大纲 `devops-practice/index.md`：模块2进度 0/7 → 4/7，总进度 22% → 37%。
commit `f7139fec`，已推送至 `origin/master`。
