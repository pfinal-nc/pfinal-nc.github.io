---
layout: page
sidebar: false
hidden: true
---

# 404 - 页面未找到

<div id="redirect-notice" style="display: none; text-align: center; padding: 30px; background: #fff3cd; border-radius: 8px; margin-bottom: 20px;">
  <p style="color: #856404; font-size: 16px; margin: 0;">
    🔄 正在为您跳转到正确的页面...
  </p>
</div>

<div style="text-align: center; padding: 50px 20px;">
  <h2 style="font-size: 72px; margin: 0;">🔍</h2>
  <h3 style="color: #666;">抱歉！页面未找到</h3>
  <p style="font-size: 18px; color: #999; margin: 20px 0;">
    您访问的页面可能已被删除、更名或暂时不可用。
  </p>
  
  <div style="margin-top: 40px;">
    <a href="/zh/" style="display: inline-block; padding: 12px 30px; background: #42b983; color: white; text-decoration: none; border-radius: 4px; margin: 0 10px;">
      🏠 返回首页
    </a>
    <a href="/zh/golang/" style="display: inline-block; padding: 12px 30px; background: #3eaf7c; color: white; text-decoration: none; border-radius: 4px; margin: 0 10px;">
      📚 浏览文章
    </a>
  </div>
  
  <div style="margin-top: 40px;">
    <h4>热门文章推荐：</h4>
    <ul style="list-style: none; padding: 0;">
      <li><a href="/zh/golang/golang提升效率的小工具.html">10个提升Golang开发效率的实用工具</a></li>
      <li><a href="/zh/golang/Go 开发终端小工具.html">Go 开发终端小工具完整指南</a></li>
      <li><a href="/zh/golang/Stop-The-World-其实没停下-Go-GC-的微暂停真相.html">Stop-The-World 其实没停下</a></li>
      <li><a href="/zh/golang/从 trace 到洞察：Go 项目的可观测性闭环实践.html">从 trace 到洞察</a></li>
    </ul>
  </div>
</div>

<style>
.content-container {
  max-width: 800px;
  margin: 0 auto;
}
</style>

<script>
(function() {
  // 仅在浏览器环境中执行（避免SSR构建时的window is not defined错误）
  if (typeof window === 'undefined') return;
  
  // 智能重定向规则（GitHub Pages不支持服务器端301重定向）
  const redirectRules = [
    // 旧的分类页面
    { from: '/archives/', to: '/zh/' },
    { from: '/categories/golang/', to: '/golang/' },
    { from: '/categories/golang/index.html', to: '/golang/' },
    { from: '/categories/工具/', to: '/zh/' },
    { from: '/categories/经验/', to: '/zh/' },
    { from: '/links/', to: '/contact' },
    
    // ========== GSC 索引问题修复：28 个"已抓取 - 尚未编入索引"页面 ==========
    
    // 1. Tools 目录 - URL 格式修复（空格和特殊字符）
    { from: '/Tools/AutoGen in Financial Data Analysis: Automating Workflows with AI Agents.html', 
      to: '/Tools/AutoGen-in-Financial-Data-Analysis-Automating-Workflows-with-AI-Agents' },
    { from: '/Tools/AutoGen-in-Financial-Data-Analysis-Automating-Workflows-with-AI-Agents.html', 
      to: '/Tools/AutoGen-in-Financial-Data-Analysis-Automating-Workflows-with-AI-Agents' },
    { from: '/Tools/Make-CLI-Tools-Brand-New-with-Golang-and-Color', 
      to: '/golang/Go-CLI-Utility-Development-Practice' },
    { from: '/Tools/Make-CLI-Tools-Brand-New-with-Golang-and-Color.html', 
      to: '/golang/Go-CLI-Utility-Development-Practice' },
    { from: '/Tools/Composer-Configuration-File-Explanation', 
      to: '/php/Composer-Configuration-File-Explanation' },
    { from: '/Tools/Composer-Configuration-File-Explanation.html', 
      to: '/php/Composer-Configuration-File-Explanation' },
    
    // 2. Golang 目录 - 移除 .html 后缀
    { from: '/golang/mastering-go-testing-advanced-techniques.html', 
      to: '/golang/mastering-go-testing-advanced-techniques' },
    { from: '/golang/high-performance-websockets-go.html', 
      to: '/golang/high-performance-websockets-go' },
    { from: '/golang/Go-Develop-Terminal-Tools.html', 
      to: '/golang/Go-Develop-Terminal-Tools' },
    { from: '/golang/Quick-Start-to-MCP-Server-Development-in-VSCode.html', 
      to: '/golang/Quick-Start-to-MCP-Server-Development-in-VSCode' },
    
    // 3. 中文 Golang URL 重定向
    { from: '/golang/提速利器：使用Go语言实现静态化API', 
      to: '/golang/Speed-Boost-Static-API-with-Golang' },
    { from: '/golang/深入Go内存分配', 
      to: '/golang/Deep-Dive-Go-Memory-Allocation' },
    
    // 4. PHP 目录 - 中文 URL 重定向
    { from: '/php/PHP进程线程解析', 
      to: '/php/PHP-Process-Thread-Analysis' },
    { from: '/php/PHP进程线程解析.html', 
      to: '/php/PHP-Process-Thread-Analysis' },
    { from: '/php/Laravel-Manual-Build', 
      to: '/php/Laravel-Manual-Build' },
    { from: '/php/Laravel-Manual-Build.html', 
      to: '/php/Laravel-Manual-Build' },
    { from: '/php/Redis配置文件详解', 
      to: '/php/Redis-Configuration-File-Explanation' },
    { from: '/php/Laravel-admin特殊路由操作', 
      to: '/php/Laravel-admin-Special-Routing-Operations' },
    { from: '/php/Laravel-Carbon-类使用', 
      to: '/php/Laravel-Carbon-Class-Usage' },
    
    // 5. 数据库目录
    { from: '/数据库/MySQL配置文件解析', 
      to: '/database/MySQL-Configuration-File-Analysis' },
    
    // 6. Wails 教程系列
    { from: '/zh/wails-tutorial-series/00-webkit-and-lifecycle.html', 
      to: '/zh/wails-tutorial-series/00-webkit-and-lifecycle' },
    
    // 7. HTTP → HTTPS 重定向（通过协议检查处理）
    // 注意：这些会在下面的协议检查中处理
    
    // 8. 查询参数页面（标签页）- 重定向到主页
    { from: '/?tag=test-driven development', to: '/' },
    { from: '/?tag=Wails&type=warning', to: '/' },
    { from: '/?tag=Laravel&type=danger', to: '/' },
    { from: '/?tag=Laravel', to: '/' },
    { from: '/?tag=PHP', to: '/' },
    
    // ========== 原有的重定向规则 ==========
    { from: '/爬虫JS逆向Webpack技巧记录.html', to: '/python/Reverse-Engineering-JS-Webpack-Tips-for-Crawlers' },
  ];
  
  // 获取当前路径（处理URL编码）
  let currentPath = window.location.pathname;
  const decodedPath = decodeURIComponent(currentPath);
  
  // 规范化路径（移除尾部斜杠，除非是根路径）
  const normalizePath = (path) => {
    if (path === '/') return '/';
    return path.replace(/\/$/, '');
  };
  
  // 检查是否需要重定向
  for (const rule of redirectRules) {
    const normalizedCurrent = normalizePath(currentPath);
    const normalizedDecoded = normalizePath(decodedPath);
    const normalizedFrom = normalizePath(rule.from);
    
    // 多种匹配方式：精确匹配、URL编码匹配、规范化匹配
    if (currentPath === rule.from || 
        decodedPath === rule.from ||
        normalizedCurrent === normalizedFrom ||
        normalizedDecoded === normalizedFrom ||
        currentPath.startsWith(rule.from) ||
        decodedPath.startsWith(rule.from)) {
      // 显示重定向提示
      const notice = document.getElementById('redirect-notice');
      if (notice) notice.style.display = 'block';
      
      // 执行重定向（使用replace避免在历史记录中留下404页面）
      setTimeout(() => {
        window.location.replace(rule.to);
      }, 500); // 减少延迟，更快重定向
      return;
    }
  }
  
  // 处理带参数的URL（?tag=, ?type=, ?category=）
  const urlParams = new URLSearchParams(window.location.search);
  if (urlParams.has('tag') || urlParams.has('type') || urlParams.has('category')) {
    window.location.href = '/';
    return;
  }
  
  // 强制HTTPS（GitHub Pages应该自动处理，但作为备份）
  if (window.location.protocol === 'http:' && window.location.hostname === 'friday-go.icu') {
    window.location.href = 'https://' + window.location.hostname + window.location.pathname + window.location.search;
    return;
  }
  
  // ========== 通用重定向处理 ==========
  
  // 1. 处理 .html 后缀（VitePress 使用 cleanUrls，不应该有 .html 后缀）
  if (currentPath.endsWith('.html') && currentPath !== '/404.html') {
    const pathWithoutHtml = currentPath.replace(/\.html$/, '');
    // 如果路径不是根路径，重定向到无 .html 后缀的版本
    if (pathWithoutHtml !== '/') {
      window.location.replace(pathWithoutHtml);
      return;
    }
  }
  
  // 2. 处理尾部斜杠（除了根路径）
  if (currentPath !== '/' && currentPath.endsWith('/')) {
    const pathWithoutSlash = currentPath.slice(0, -1);
    window.location.replace(pathWithoutSlash);
    return;
  }
  
  // 3. 处理大小写问题（如果路径包含大写字母，尝试小写版本）
  // 注意：这个规则比较激进，只在明确知道应该小写的情况下使用
  // 如果路径包含中文或特殊字符，跳过此规则
  if (currentPath !== '/' && /[A-Z]/.test(currentPath) && !/[\u4e00-\u9fa5]/.test(currentPath)) {
    const lowerPath = currentPath.toLowerCase();
    // 只在路径明显不同时才重定向（避免循环）
    if (lowerPath !== currentPath) {
      // 延迟执行，避免与其他规则冲突
      setTimeout(() => {
        window.location.replace(lowerPath);
      }, 100);
      return;
    }
  }
})();
</script>

